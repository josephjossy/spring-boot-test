CREATE TABLE if NOT EXISTS books(
   id BIGINT,
   name VARCHAR(50),
   author VARCHAR(50)
);

insert into books values
    (100, 'Whale rider', 'Richard cox'),
    (101, 'Beavers of City', 'Wilson churchil');    

